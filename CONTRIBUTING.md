# Contributing (VERIFRAX-SAMPLES)

This repo contains executable examples.
Rules:
- keep examples minimal and deterministic
- pin tool versions where possible
- no secrets, no tokens, no external side effects
